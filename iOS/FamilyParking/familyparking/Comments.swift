//
//  Comments.swift
//  familyparking
//
//  Created by mauro piva on 19/02/15.
//  Copyright (c) 2015 familyParking. All rights reserved.
//

import Foundation

/**
ciao, sono mauro

let prefs:NSUserDefaults = NSUserDefaults.standardUserDefaults()
let isLoggedIn:Int = prefs.integerForKey("ISLOGGEDIN") as Int
if (isLoggedIn != 1) {
self.performSegueWithIdentifier("goto_login", sender: self)
} else {
getGroups()
}

ISLOGGEDIN is   0 if user is NOT logged in
                1 otherwise

EMAIL           is the inserted EMAIL
                "" otherwise

USERNAME        is the inserted USERNAME
                "" otherwise

PIN             is the received PIN
                ""

DOING 
show groups names

*/

class Comments {
    
    let serverPath = "http://ios01.first-vision-798.appspot.com/"
    
}

